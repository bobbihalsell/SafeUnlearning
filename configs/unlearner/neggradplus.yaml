name: neggradplus
evaluate: true
cfg:
  optimizer: sgd
  lr: 0.001
  momentum: 
  lr_decay_factor:
  epochs_per_lr_decay:
  weight_decay: 0.0005
  use_l2_penalty: false
  epochs: 4
  beta: 0.99  # Weightage between retain/forget loss importance. Value between 0 to 1.