evaluate: true
model_save_dir: # Model save dir. Empty – save to ./artifacts
cfg:
  optimizer: sgd
  lr: 0.01
  lr_decay_factor: 0.1
  epochs_per_lr_decay: 90
  momentum: 0.9
  weight_decay: 0.0005
  use_l2_penalty: false
  epochs: 2