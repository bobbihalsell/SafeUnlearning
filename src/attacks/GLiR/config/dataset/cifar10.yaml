name: cifar10  # Options: cifar10, cifar5, cifar100, imagenet
num_classes: 10
save_path: ./data # the directory filepath where the dataset should be loaded in from. 
background_ratio: 0.1 # the ratio of background data in the dataset.
