name: neggradplus
evaluate: true
cfg:
  optimizer: adam
  lr: 0.001
  lr_decay_factor: 0.1
  epochs_per_lr_decay: 2
  weight_decay: 0
  use_l2_penalty: false
  epochs: 2
  beta: 0.99  # Weightage between retain/forget loss importance. Value between 0 to 1.