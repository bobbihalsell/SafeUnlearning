name: neggradplus
evaluate: true
cfg:
  optimizer: sgd
  lr: 0.0005
  lr_decay_factor: 0.1
  epochs_per_lr_decay: 2
  weight_decay: 0.0001
  use_l2_penalty: false
  epochs: 4
  beta: 0.995  # Weightage between retain/forget loss importance. Value between 0 to 1.