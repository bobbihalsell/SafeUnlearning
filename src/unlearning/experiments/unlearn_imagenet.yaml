model:
  name: mobilenet_v2
  num_classes: 200
  model_ckpt_path: ./artifacts/models/mobilenet_v2_42_original.pt  # You must specify a path to the original model.
unlearner:
  name: neggradplus  # Options: neggrad, neggradplus, scrub, euk, cfk, finetune
  evaluate: true
  cfg:
    # Common parameters for all unlearners
    lr: 0.01
    weight_decay: 0
    use_l2_penalty: false
    epochs: 1 # Ignored for scrub
    beta: 0.95  # For neggradplus  beta = 0: forget only, beta = 1: retain only
dataset:
  name: imagenet  # Options: cifar10, cifar5, cifar100, imagenet
  save_path: ./imagenet # the directory filepath where the dataset should be loaded in from. 
  cfg:
    batch_sizes:
      retain: 64
      forget: 64
      val: 64
    num_workers: 4
seed: 42
output_dir: ./artifacts
verbose: true