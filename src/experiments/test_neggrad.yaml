model: 
  name: resnet18
  pretrained: false
  num_classes: 10
unlearner:
  name: neggrad
  cfg:
    num_epochs: 3
    lr: 0.01
    weight_decay: 0
    use_l2_penalty: false
    evaluate: true
dataset:
  name: cifar10
  val_ratio: 0.0
forget_method:
  name: index
  parameters: [0]
seed: 40
batch_size: 32